'use client';
import Link from 'next/link';
import { useEffect,useState } from 'react';
import { createClient } from '@/lib/supabase/client';
const money=(n:number)=>new Intl.NumberFormat('en-BD',{maximumFractionDigits:2}).format(Number(n)||0);
export default function Dashboard(){const s=createClient();const[main,setMain]=useState(0);const[secondary,setSecondary]=useState(0);const[loading,setLoading]=useState(true);useEffect(()=>{Promise.all([s.from('transactions').select('id',{count:'exact',head:true}).eq('business_code','main'),s.from('transactions').select('id',{count:'exact',head:true}).eq('business_code','secondary')]).then(([a,b])=>{setMain(a.count||0);setSecondary(b.count||0);setLoading(false)})},[]);return <main style={{minHeight:'100vh',background:'#f6f7f3',padding:30,fontFamily:'Inter,system-ui,sans-serif',color:'#101713'}}><div style={{maxWidth:1000,margin:'auto'}}><div style={{marginBottom:30}}><div style={{fontSize:11,fontWeight:800,letterSpacing:'.12em',color:'#166534'}}>A P TRADERS</div><h1>Business Control Center</h1><p style={{color:'#68726c'}}>Choose a business ledger. Main Business and Secondary Business are kept separate.</p></div><div style={{display:'grid',gridTemplateColumns:'repeat(2,1fr)',gap:18}}><Link href="/main" style={{textDecoration:'none',color:'inherit',background:'#fff',border:'1px solid #e5e7eb',borderRadius:18,padding:28}}><div style={{fontSize:12,fontWeight:800,letterSpacing:'.1em'}}>MAIN BUSINESS</div><h2>Main Business</h2><p style={{color:'#68726c'}}>Primary ledger. Only transactions marked <strong>main</strong> are shown.</p><div style={{fontSize:26,fontWeight:800}}>{loading?'…':main} <span style={{fontSize:12,fontWeight:500}}>transactions</span></div><div style={{marginTop:18,display:'inline-block',padding:'10px 14px',border:'1px solid #d7ddd8',borderRadius:9}}>Open Main Business →</div></Link><Link href="/secondary" style={{textDecoration:'none',color:'inherit',background:'#fff',border:'1px solid #e5e7eb',borderRadius:18,padding:28}}><div style={{fontSize:12,fontWeight:800,letterSpacing:'.1em'}}>SECONDARY BUSINESS</div><h2>Secondary Business</h2><p style={{color:'#68726c'}}>Buying, Selling, People’s Money, Owner Capital and FIFO.</p><div style={{fontSize:26,fontWeight:800}}>{loading?'…':secondary} <span style={{fontSize:12,fontWeight:500}}>transactions</span></div><div style={{marginTop:18,display:'inline-block',padding:'10px 14px',border:'1px solid #d7ddd8',borderRadius:9}}>Open Secondary Business →</div></Link></div></div></main>;}