'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import { createClient } from '@/lib/supabase/client';
const money=(n:number)=>new Intl.NumberFormat('en-BD',{maximumFractionDigits:2}).format(Number(n)||0);
type K={selling_revenue:number;realized_selling_profit:number;returns_paid:number;expenses:number;net_profit:number;people_money_funded:number;available_owner_capital:number};
export default function ProfitLoss(){const s=createClient();const[k,setK]=useState<K|null>(null);const[e,setE]=useState('');useEffect(()=>{s.from('v_business_kpis').select('*').single().then(({data,error})=>{setK(data as K);if(error)setE(error.message)})},[]);if(!k)return <main style={{padding:32}}>Loading P&amp;L…{e&&` ${e}`}</main>;const rows:[string,number][]=[['Selling Revenue',k.selling_revenue],['Realized Selling Profit',k.realized_selling_profit],['Investor Returns Paid',-k.returns_paid],['Expenses',-k.expenses],['Net Profit',k.net_profit]];return <main style={{minHeight:'100vh',background:'#f6f7f3',padding:30,fontFamily:'Inter,system-ui,sans-serif',color:'#101713'}}><div style={{maxWidth:1000,margin:'auto'}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'end',marginBottom:22}}><div><div style={{fontSize:11,fontWeight:800,letterSpacing:'.12em',color:'#166534'}}>A P TRADERS · SECONDARY BUSINESS</div><h1 style={{margin:'6px 0'}}>Profit &amp; Loss</h1><p style={{margin:0,color:'#68726c'}}>Revenue, realized trading profit, investor returns and expenses.</p></div><Link href="/secondary" style={{padding:'9px 12px',border:'1px solid #d7ddd8',borderRadius:9,background:'#fff',color:'#101713',textDecoration:'none'}}>← Dashboard</Link></header><section style={{background:'#fff',border:'1px solid #e5e7eb',borderRadius:16,padding:24}}><h2 style={{marginTop:0}}>P&amp;L Waterfall</h2>{rows.map(([label,value])=><div key={label} style={{display:'flex',justifyContent:'space-between',padding:'15px 0',borderBottom:'1px solid #edf0ed',fontWeight:label==='Net Profit'?800:500}}><span>{label}</span><span>৳{money(value)}</span></div>)}<div style={{marginTop:18,padding:15,borderRadius:12,background:'#eaf4ec',fontSize:12}}>People’s Money principal is a liability/capital-provider balance and is not counted as profit. Investor return payments reduce profit; principal payments do not.</div></section></div></main>}
