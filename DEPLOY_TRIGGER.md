# P-S Enterprise deployment trigger

This file intentionally contains no application logic. It triggers a fresh Vercel production build after removing the obsolete `BusinessEnhancements.tsx` component.
